import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app was built using React, Redux, React Router, and many other
      helpful libraries.
    </p>
  </div>
);

export default AboutPage;
